{
	// Change Tick Rate for Living Entities | Default: trueDoesn’t affect Player Entities
	"getChangeTickRateLivingEntities": true,
	// Change Tick Rate for Player's Vehicle Entities | Default: falseOnly when player is mounted
	"getChangeTickRateVehicleEntities": false,
	// Change Tick Rate for Hostile Entities | Default: true
	"getChangeTickRateHostileEntities": true,
	// Change Tick Rate based on TPS for Item Entities | Default: true
	"getChangeTickRateItemEntities": false,
	// Change Tick Rate based on TPS for Block Entities | Default: false
	"getChangeTickRateBlockEntities": false,
	// Change Tick Rate based on TPS for Nether Portal Blocks | Default: true
	"getChangeTickRateNetherPortalBlock": false,
	// Change mode 0 = Weak Mode1 = Intermediate Mode2 = Strong Mode| Default: 0
	"performanceMode": 2,
	// Specific Tick Rate for Living EntitiesIf set to 0, it will do the Tick Rate based on TPS
	"specificTickRateLivingEntities": 0,
	// Specific Tick Rate for Item EntitiesIf set to 0, it will do the Tick Rate based on TPS
	"specificTickRateItemEntities": 0,
	// Specific Tick Rate for Block EntitiesIf set to 0, it will do the Tick Rate based on TPS
	"specificTickRateBlockEntities": 0,
	// Specific Tick Rate for Nether Portal BlocksIf set to 0, it will do the Tick Rate based on TPS
	"specificTickRateNetherPortalBlocks": 0,
	// Distance to Detect Other Item Entities | Default: 3.0 blocksMin Distance: 0.5 blocks / Max Distance: 10 blocksIf there is ServerCore, this is disabled, change it in ServerCore configs
	"distanceItemEntities": 3.0,
	// Max Spawn Mobs from Spawners | Default: 6 Mobs
	"maxSpawnerMobs": 6,
	// Spawn Range around a Spawner Block | Default: 4 Blocks
	"spawnerRange": 4,
	// Monster SpawnGroup Max Capacity | Default: 70After Changes you need to Restart the Server
	"monsterSpawnGroupCapacity": 70,
	// Creature SpawnGroup Max Capacity | Default: 10After Changes you need to Restart the Server
	"creatureSpawnGroupCapacity": 10,
	// Ambient SpawnGroup Max Capacity | Default: 15After Changes you need to Restart the Server
	"ambientSpawnGroupCapacity": 15,
	// Axolotls / Underground Water Creature / Water Creature SpawnGroup Max Capacity | Default: 5After Changes you need to Restart the Server
	"value5SpawnGroupCapacity": 5,
	// Water Ambient SpawnGroup Max Capacity | Default: 20After Changes you need to Restart the Server
	"waterAmbientSpawnGroupCapacity": 20
}